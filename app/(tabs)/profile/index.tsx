import { StyleSheet, Text, View } from 'react-native';
import React from 'react';
import ProfileScreen from '@/app/common-flow/profile';

const Profile = () => {
  return <ProfileScreen />;
};

export default Profile;

const styles = StyleSheet.create({});
